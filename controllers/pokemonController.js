import { PokemonModel } from "../models/mysql/pokemon.js";

export class PokemonController {
  static async createPokemom(req, res, next) {
    const create = await TeamModel.createTeam();
  }

  static async deletePokemon(req, res, next) {}
}
